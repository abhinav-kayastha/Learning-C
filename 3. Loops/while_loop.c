#include <stdio.h>

int main()
{
    /*
    syntax:

    while (condition)
    {
        code to be exevuted
    }
    */
    int x = 0;

    while (x <= 5)
    {
        printf("%d\n", x);
        x++;
    }
}