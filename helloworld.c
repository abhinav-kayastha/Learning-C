#include <stdio.h>

/* A preprocesser command that tells the compiler to 
include contents of the file "standard input output",
which contains useful functions for input and output
*/

int main() // Main function
{
    /* Anything inside the main func. is read
    procedurally
    */

    printf("I like pizza.\n");
    printf("It's really good.");
    return 0;

    /* Termination of statements returns 0 when 
    program runs succesfully
    */
}